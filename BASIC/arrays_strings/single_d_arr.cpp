#include <bits/stdc++.h>
using namespace std;

int main() {
    int arr[4];
    arr[0] = 4;
    arr[3] = 6;

    cout << arr[0];

    // int len = sizeof(arr) / sizeof(arr[0]); // CORRECT

    // for (int i = 0; i < len; i++) {
    //     cout << arr[i] << " ";
    // }

    // cout << "\n";
    return 0;
}
